import styled from '@emotion/styled';

export const ListWrapper = styled.ul`
  margin: 0;
  padding: 0;
  li {
    margin: 0;
    padding: 0 0 8px 0;
    list-style: none;

    display: flex;
    align-items: center;
    button {
        margin-left: auto;
        margin-right: 0;
    }
  }
`;
